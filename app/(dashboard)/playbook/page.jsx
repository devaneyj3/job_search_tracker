import FollowUpDashboard from "@/features/dashboard/FollowUpDashboard";

export const metadata = {
	title: "Follow-up Playbook | Job Tracker",
	description: "Research-backed guidance for job application follow-ups",
};

export default function PlaybookPage() {
	return <FollowUpDashboard />;
}
